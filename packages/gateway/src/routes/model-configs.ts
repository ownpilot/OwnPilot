/**
 * AI Model Configs API Routes
 *
 * Re-exports from the model-configs/ directory.
 * Preserves the existing import surface so consumers don't need changes.
 */

export { modelConfigsRoutes } from './model-configs/index.js';
