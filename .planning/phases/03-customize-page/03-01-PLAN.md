---
phase: 03-customize-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/ui/src/constants/nav-descriptions.ts
  - packages/ui/src/pages/CustomizePage.tsx
autonomous: true
requirements: [CZ-01, CZ-02, CZ-03, CZ-04, CZ-05]

must_haves:
  truths:
    - "Navigating to /customize shows all 56 nav items grouped into their categories"
    - "Each card displays the item icon, label, and a short description"
    - "Clicking a card (or its pin button) toggles its pinned state — the card immediately shows filled vs outline pin icon"
    - "Typing in the search bar instantly filters visible cards by label (no API call)"
    - "Pin counter shows 'X / 15 slots used' and updates with each pin/unpin"
    - "When pin count is already 15, attempting to pin another item fires a warning toast and the item is NOT added"
    - "Pinned items (other than /customize) immediately appear in the sidebar when user navigates away"
  artifacts:
    - path: "packages/ui/src/constants/nav-descriptions.ts"
      provides: "Description map for all 56 nav items keyed by route path"
      exports: ["NAV_DESCRIPTIONS"]
    - path: "packages/ui/src/pages/CustomizePage.tsx"
      provides: "Full /customize page: categorized grid, search, pin toggle, pin counter"
      exports: ["CustomizePage"]
  key_links:
    - from: "packages/ui/src/pages/CustomizePage.tsx"
      to: "packages/ui/src/constants/nav-descriptions.ts"
      via: "import { NAV_DESCRIPTIONS }"
      pattern: "NAV_DESCRIPTIONS"
    - from: "packages/ui/src/pages/CustomizePage.tsx"
      to: "packages/ui/src/hooks/usePinnedItems.ts"
      via: "import { usePinnedItems }"
      pattern: "usePinnedItems"
    - from: "packages/ui/src/pages/CustomizePage.tsx"
      to: "packages/ui/src/constants/nav-items.ts"
      via: "import { navGroups, mainItems, bottomItems }"
      pattern: "navGroups"
---

<objective>
Create the nav-descriptions constant and the full CustomizePage component — the categorized card grid where users can pin and unpin navigation items.

Purpose: This delivers the core Customize UX: users browse all 56 nav items grouped by category, toggle pins, search/filter, and see a live pin counter. Changes write to localStorage via usePinnedItems and the Sidebar immediately reflects them on next render.

Output:
- packages/ui/src/constants/nav-descriptions.ts — short description map for all 56 items
- packages/ui/src/pages/CustomizePage.tsx — full page component (grid + search + pin toggle + counter + toast on limit)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/ayaz/ownpilot/.planning/ROADMAP.md
@/home/ayaz/ownpilot/.planning/REQUIREMENTS.md
@/home/ayaz/ownpilot/.planning/phases/02-sidebar-rebuild/02-01-SUMMARY.md

<interfaces>
<!-- Key interfaces the executor needs. No codebase exploration required. -->

From packages/ui/src/constants/nav-items.ts:
```typescript
export interface NavItem {
  to: string;
  icon: React.ComponentType<{ className?: string }>;
  label: string;
}

export interface NavGroup {
  id: string;
  label: string;
  icon: React.ComponentType<{ className?: string }>;
  items: NavItem[];
  defaultOpen?: boolean;
  badge?: string;
}

export const mainItems: NavItem[]   // 5 items: Chat /, Dashboard, Analytics, Channels, Conversations
export const navGroups: NavGroup[]  // 6 groups: data(8), ai(9), tools(4), system(9), experimental(5), settings(14)
export const bottomItems: NavItem[] // 2 items: About /about, Profile /profile
export const ALL_NAV_ITEMS: NavItem[] // flat array: [...mainItems, ...navGroups.flatMap(g=>g.items), ...bottomItems]
```

From packages/ui/src/hooks/usePinnedItems.ts:
```typescript
export const MAX_PINNED_ITEMS = 15;
// usePinnedItems returns:
//   pinnedItems: string[]   — array of pinned route paths
//   setPinnedItems: (updater: string[] | ((prev: string[]) => string[])) => void
//   MAX_PINNED_ITEMS: 15
export function usePinnedItems(): {
  pinnedItems: string[];
  setPinnedItems: (updater: string[] | ((prev: string[]) => string[])) => void;
  MAX_PINNED_ITEMS: number;
};
```

From packages/ui/src/components/ToastProvider.tsx:
```typescript
// Usage:
//   const toast = useToast();
//   toast.warning('Pin limit reached — max 15 items');
export function useToast(): {
  success: (message: string, title?: string) => void;
  error: (message: string, title?: string) => void;
  warning: (message: string, title?: string) => void;
  info: (message: string, title?: string) => void;
};
```

All nav items — exact route paths (needed for NAV_DESCRIPTIONS keys):
mainItems: /, /dashboard, /analytics, /channels, /history
data group: /tasks, /notes, /calendar, /contacts, /bookmarks, /expenses, /habits, /pomodoro
ai group: /memories, /goals, /plans, /triggers, /workflows, /autonomous, /artifacts, /approvals, /autonomy
tools group: /tools, /custom-tools, /skills, /plugins
system group: /agents, /models, /wizards, /workspaces, /custom-data, /data-browser, /costs, /logs, /event-monitor
experimental group: /claws, /fleet, /edge-devices, /coding-agents, /orchestration
settings group: /settings/api-keys, /settings/providers, /settings/ai-models, /settings/model-routing,
               /settings/security, /settings/security-scanner, /settings/tool-groups,
               /settings/cli-tools, /settings/coding-agents, /settings/mcp-servers,
               /settings/connected-apps, /settings/workflow-tools, /settings/config-center, /settings/system
bottomItems: /about, /profile

Tailwind design tokens in use (from DashboardPage.tsx reference):
  bg-bg-secondary dark:bg-dark-bg-secondary
  bg-bg-tertiary dark:bg-dark-bg-tertiary
  text-text-primary dark:dark-text-primary
  text-text-secondary dark:dark-text-secondary
  text-text-muted dark:dark-text-muted
  border-border dark:dark-border
  text-primary  (brand color)
  bg-primary/10 (pin active background)
  border-primary (pin active border)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create nav-descriptions.ts constant</name>
  <files>packages/ui/src/constants/nav-descriptions.ts</files>
  <read_first>
    - packages/ui/src/constants/nav-items.ts (all 56 route paths — Task 2 will import NAV_DESCRIPTIONS)
  </read_first>
  <action>
Create `packages/ui/src/constants/nav-descriptions.ts` with the following exact content.
Do NOT use the Write tool blindly — confirm the route paths below match nav-items.ts before writing.

The file must export a single `Record<string, string>` called `NAV_DESCRIPTIONS` containing a
one-sentence description for every route path in ALL_NAV_ITEMS (56 entries).
The /customize path itself is NOT in ALL_NAV_ITEMS — do NOT add it.

Exact content to write:

```typescript
/**
 * Short descriptions for every nav item — displayed in the Customize page grid.
 * Keys are route paths (matching nav-items.ts). One sentence, ≤ 10 words each.
 */
export const NAV_DESCRIPTIONS: Record<string, string> = {
  // Main items
  '/': 'Start or continue an AI conversation',
  '/dashboard': 'Overview of your tasks, notes, and agents',
  '/analytics': 'Usage costs, token counts, and model stats',
  '/channels': 'Connected messaging channels and integrations',
  '/history': 'Browse and search past conversations',

  // Personal Data
  '/tasks': 'Create, track, and complete your tasks',
  '/notes': 'Capture and organize your notes',
  '/calendar': 'View and manage calendar events',
  '/contacts': 'Store and browse your contacts',
  '/bookmarks': 'Save and organize important links',
  '/expenses': 'Track your spending and expenses',
  '/habits': 'Build and track daily habits',
  '/pomodoro': 'Focus timer with Pomodoro technique',

  // AI & Automation
  '/memories': 'Long-term facts the AI remembers about you',
  '/goals': 'Set and track personal or project goals',
  '/plans': 'AI-generated step-by-step action plans',
  '/triggers': 'Automate actions on events or schedules',
  '/workflows': 'Build and run multi-step automations',
  '/autonomous': 'Manage background AI agents',
  '/artifacts': 'Outputs and files generated by AI',
  '/approvals': 'Review and approve pending AI actions',
  '/autonomy': 'Configure AI autonomy and trust levels',

  // Tools & Extensions
  '/tools': 'Browse available AI tools and functions',
  '/custom-tools': 'Define your own JavaScript AI tools',
  '/skills': 'Install and manage skill packages',
  '/plugins': 'Extend OwnPilot with community plugins',

  // System
  '/agents': 'Configure and manage AI agent profiles',
  '/models': 'View and test available AI models',
  '/wizards': 'Guided setup flows for new features',
  '/workspaces': 'Manage file and project workspaces',
  '/custom-data': 'Define and query your custom data types',
  '/data-browser': 'Inspect raw data tables in your database',
  '/costs': 'Track AI usage costs and budgets',
  '/logs': 'View system and agent activity logs',
  '/event-monitor': 'Monitor real-time system events',

  // Experimental
  '/claws': 'Autonomous continuous AI agent runners',
  '/fleet': 'Coordinate fleets of parallel AI workers',
  '/edge-devices': 'Manage IoT and edge device connections',
  '/coding-agents': 'AI agents that write and run code',
  '/orchestration': 'Coordinate complex multi-agent tasks',

  // Settings
  '/settings/api-keys': 'Manage your LLM provider API keys',
  '/settings/providers': 'Configure AI provider connections',
  '/settings/ai-models': 'Set default and fallback AI models',
  '/settings/model-routing': 'Route tasks to the best model automatically',
  '/settings/security': 'Password, auth, and access control',
  '/settings/security-scanner': 'Scan for security vulnerabilities',
  '/settings/tool-groups': 'Organize tools into permission groups',
  '/settings/cli-tools': 'Register CLI tools for AI use',
  '/settings/coding-agents': 'Configure coding agent environments',
  '/settings/mcp-servers': 'Connect Model Context Protocol servers',
  '/settings/connected-apps': 'Link external apps and services',
  '/settings/workflow-tools': 'Tools available inside workflow nodes',
  '/settings/config-center': 'Global OwnPilot configuration',
  '/settings/system': 'System info, storage, and diagnostics',

  // Bottom items
  '/about': 'About OwnPilot, version, and credits',
  '/profile': 'Your profile, preferences, and account',
};
```
  </action>
  <acceptance_criteria>
    - grep -c "'/": file contains at least 54 key entries (one per nav item)
    - grep "export const NAV_DESCRIPTIONS": export exists
    - grep "Record\<string, string\>": TypeScript type annotation present
  </acceptance_criteria>
  <verify>
    <automated>cd /home/ayaz/ownpilot && npx tsc --noEmit -p packages/ui/tsconfig.json 2>&1 | grep -E "nav-descriptions|error" | head -10 || echo "typecheck clean"</automated>
  </verify>
  <done>nav-descriptions.ts exists with 56 route path keys, exports NAV_DESCRIPTIONS as Record&lt;string, string&gt;, TypeScript compiles clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create CustomizePage.tsx</name>
  <files>packages/ui/src/pages/CustomizePage.tsx</files>
  <read_first>
    - packages/ui/src/constants/nav-items.ts (navGroups structure, NavGroup/NavItem types)
    - packages/ui/src/constants/nav-descriptions.ts (just created in Task 1)
    - packages/ui/src/hooks/usePinnedItems.ts (hook API)
    - packages/ui/src/components/ToastProvider.tsx (useToast API — toast.warning())
    - packages/ui/src/pages/DashboardPage.tsx (Tailwind class patterns to follow)
  </read_first>
  <action>
Create `packages/ui/src/pages/CustomizePage.tsx` implementing the full Customize page.

**Architecture:**
- Single-file component, no sub-components extracted
- Uses navGroups (6 groups) + mainItems (5 items as "Main" group) + bottomItems (2 items as "Other" group)
- /customize route itself is EXCLUDED from the grid (it's always in sidebar, never pinnable)
- State: `query` (string, controlled search input)
- Data: pinnedItems from usePinnedItems(), MAX_PINNED_ITEMS = 15

**Full component structure:**

```tsx
import { useState, useMemo } from 'react';
import { Pin } from '../components/icons';
import { navGroups, mainItems, bottomItems } from '../constants/nav-items';
import type { NavItem, NavGroup } from '../constants/nav-items';
import { NAV_DESCRIPTIONS } from '../constants/nav-descriptions';
import { usePinnedItems } from '../hooks/usePinnedItems';
import { useToast } from '../components/ToastProvider';
import { Search } from '../components/icons';

// Build display sections — prepend synthetic "Main" group, append synthetic "Other" group
// Exclude /customize from all groups
const DISPLAY_SECTIONS: NavGroup[] = [
  {
    id: 'main',
    label: 'Main',
    icon: /* LayoutDashboard */ ...,
    items: mainItems.filter(item => item.to !== '/customize'),
  },
  ...navGroups,
  {
    id: 'other',
    label: 'Other',
    icon: /* Info */ ...,
    items: bottomItems,
  },
];

export function CustomizePage() {
  const { pinnedItems, setPinnedItems, MAX_PINNED_ITEMS } = usePinnedItems();
  const toast = useToast();
  const [query, setQuery] = useState('');

  const normalizedQuery = query.trim().toLowerCase();

  const filteredSections = useMemo(() => {
    if (!normalizedQuery) return DISPLAY_SECTIONS;
    return DISPLAY_SECTIONS.map(section => ({
      ...section,
      items: section.items.filter(item =>
        item.label.toLowerCase().includes(normalizedQuery)
      ),
    })).filter(section => section.items.length > 0);
  }, [normalizedQuery]);

  const handleTogglePin = (path: string) => {
    const isPinned = pinnedItems.includes(path);
    if (!isPinned && pinnedItems.length >= MAX_PINNED_ITEMS) {
      toast.warning(`Pin limit reached — max ${MAX_PINNED_ITEMS} items`);
      return;
    }
    setPinnedItems(prev =>
      isPinned ? prev.filter(p => p !== path) : [...prev, path]
    );
  };

  // Pinned count excludes /customize (always pinned separately, not user-controlled)
  const userPinnedCount = pinnedItems.filter(p => p !== '/customize').length;

  return (
    <div className="flex flex-col h-full">
      {/* Page header */}
      <header className="flex items-center justify-between px-6 py-4 border-b border-border dark:border-dark-border">
        <div>
          <h2 className="text-lg font-semibold text-text-primary dark:text-dark-text-primary">
            Customize Sidebar
          </h2>
          <p className="text-sm text-text-muted dark:text-dark-text-muted">
            Pin pages to your sidebar for quick access
          </p>
        </div>
        {/* Pin counter */}
        <div className="flex items-center gap-2 text-sm text-text-muted dark:text-dark-text-muted">
          <Pin className="w-4 h-4" />
          <span data-testid="pin-counter">
            <span className={userPinnedCount >= MAX_PINNED_ITEMS ? 'text-error font-semibold' : 'text-text-primary dark:text-dark-text-primary font-semibold'}>
              {userPinnedCount}
            </span>
            {' / '}{MAX_PINNED_ITEMS} slots used
          </span>
        </div>
      </header>

      {/* Search bar */}
      <div className="px-6 py-3 border-b border-border dark:border-dark-border">
        <div className="relative">
          <Search className="absolute left-3 top-1/2 -translate-y-1/2 w-4 h-4 text-text-muted dark:text-dark-text-muted" />
          <input
            type="text"
            placeholder="Search pages..."
            value={query}
            onChange={e => setQuery(e.target.value)}
            data-testid="customize-search"
            className="w-full pl-9 pr-4 py-2 text-sm bg-bg-tertiary dark:bg-dark-bg-tertiary border border-border dark:border-dark-border rounded-lg text-text-primary dark:text-dark-text-primary placeholder-text-muted dark:placeholder-dark-text-muted focus:outline-none focus:border-primary focus:ring-1 focus:ring-primary/30 transition-colors"
          />
        </div>
      </div>

      {/* Grid content */}
      <div className="flex-1 overflow-y-auto p-6 space-y-8" data-testid="customize-grid">
        {filteredSections.length === 0 ? (
          <div className="text-center py-12 text-text-muted dark:text-dark-text-muted text-sm">
            No pages match "{query}"
          </div>
        ) : (
          filteredSections.map(section => (
            <section key={section.id} data-testid={`customize-group-${section.id}`}>
              {/* Group header */}
              <div className="flex items-center gap-2 mb-3">
                <section.icon className="w-4 h-4 text-text-muted dark:text-dark-text-muted" />
                <h3 className="text-xs font-semibold text-text-muted dark:text-dark-text-muted uppercase tracking-wider">
                  {section.label}
                </h3>
                {section.badge && (
                  <span className="px-1.5 py-0.5 text-[10px] font-medium bg-primary/10 text-primary rounded">
                    {section.badge}
                  </span>
                )}
              </div>

              {/* Items grid: 1 col mobile, 2 col tablet, 3-4 col desktop */}
              <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-3">
                {section.items.map(item => {
                  const isPinned = pinnedItems.includes(item.to);
                  const Icon = item.icon;
                  return (
                    <button
                      key={item.to}
                      data-testid={`customize-card-${item.to.replace(/\//g, '-').replace(/^-/, '')}`}
                      onClick={() => handleTogglePin(item.to)}
                      className={`group relative flex items-start gap-3 p-4 rounded-xl border text-left transition-all ${
                        isPinned
                          ? 'bg-primary/10 border-primary shadow-sm'
                          : 'bg-bg-tertiary dark:bg-dark-bg-tertiary border-border dark:border-dark-border hover:border-primary/50 hover:bg-bg-secondary dark:hover:bg-dark-bg-secondary'
                      }`}
                      aria-pressed={isPinned}
                      aria-label={`${isPinned ? 'Unpin' : 'Pin'} ${item.label}`}
                    >
                      {/* Icon */}
                      <div className={`w-9 h-9 rounded-lg flex items-center justify-center shrink-0 ${
                        isPinned ? 'bg-primary/20' : 'bg-bg-secondary dark:bg-dark-bg-secondary'
                      }`}>
                        <Icon className={`w-4 h-4 ${isPinned ? 'text-primary' : 'text-text-secondary dark:text-dark-text-secondary'}`} />
                      </div>

                      {/* Label + description */}
                      <div className="flex-1 min-w-0">
                        <p className={`text-sm font-medium truncate ${
                          isPinned ? 'text-primary' : 'text-text-primary dark:text-dark-text-primary'
                        }`}>
                          {item.label}
                        </p>
                        <p className="text-xs text-text-muted dark:text-dark-text-muted line-clamp-2 mt-0.5">
                          {NAV_DESCRIPTIONS[item.to] ?? ''}
                        </p>
                      </div>

                      {/* Pin indicator — top-right corner */}
                      <div className={`absolute top-3 right-3 transition-opacity ${
                        isPinned ? 'opacity-100' : 'opacity-0 group-hover:opacity-60'
                      }`}>
                        <Pin className={`w-3.5 h-3.5 ${isPinned ? 'text-primary fill-primary' : 'text-text-muted dark:text-dark-text-muted'}`} />
                      </div>
                    </button>
                  );
                })}
              </div>
            </section>
          ))
        )}
      </div>
    </div>
  );
}
```

**Implementation notes:**
- Import `LayoutDashboard` from `'../components/icons'` for the synthetic "Main" group icon.
- Import `Info` from `'../components/icons'` for the synthetic "Other" group icon.
- Import `Pin`, `Search` from `'../components/icons'`.
- Confirm `Pin` is exported from `packages/ui/src/components/icons.tsx` before using it. If `Pin` is NOT in icons.tsx, use `Bookmark` (fill on pin) as fallback and document the deviation.
- The DISPLAY_SECTIONS constant must be defined at module level (outside the component) — it is static.
- `userPinnedCount` counts pinnedItems excluding '/customize' because /customize is always visible in sidebar regardless of pin state.
- The `fill-primary` class on the pin icon when pinned — check if this Tailwind class exists in the project. If it does not, use `text-primary` instead and set fill via a style attribute: `style={{ fill: 'currentColor' }}`.
  </action>
  <acceptance_criteria>
    - grep "export function CustomizePage": named export exists
    - grep "usePinnedItems": hook imported and used
    - grep "useToast": toast hook imported and used
    - grep "NAV_DESCRIPTIONS": description map imported and used
    - grep "data-testid=\"customize-search\"": search input has testid
    - grep "data-testid=\"pin-counter\"": counter has testid
    - grep "data-testid=\"customize-grid\"": grid wrapper has testid
    - grep "pin-limit\|Pin limit\|max.*15\|MAX_PINNED_ITEMS": pin limit toast fired
    - grep "normalizedQuery\|trim().toLowerCase": search normalization present
    - grep "aria-pressed": accessibility attribute on card buttons
  </acceptance_criteria>
  <verify>
    <automated>cd /home/ayaz/ownpilot && npx tsc --noEmit -p packages/ui/tsconfig.json 2>&1 | grep -E "CustomizePage|nav-descriptions|error TS" | head -15 || echo "typecheck clean"</automated>
  </verify>
  <done>CustomizePage.tsx exists, exports CustomizePage function, TypeScript compiles clean with zero new errors. Component uses usePinnedItems, useToast, NAV_DESCRIPTIONS, navGroups. Pin counter shows correctly. Search input with testid present. All 56 nav items (minus /customize) appear in grid. Pin limit toast fires at 15.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. TypeScript clean: `cd /home/ayaz/ownpilot && npx tsc --noEmit -p packages/ui/tsconfig.json 2>&1 | grep "error TS" | head -10`
2. NAV_DESCRIPTIONS has entries for all routes: `grep -c "'/'" packages/ui/src/constants/nav-descriptions.ts`
3. CustomizePage exports correctly: `grep "export function CustomizePage" packages/ui/src/pages/CustomizePage.tsx`
4. usePinnedItems wired: `grep "usePinnedItems\|pinnedItems" packages/ui/src/pages/CustomizePage.tsx`
5. Toast on limit: `grep -i "pin limit\|MAX_PINNED_ITEMS\|toast.warning" packages/ui/src/pages/CustomizePage.tsx`
</verification>

<success_criteria>
- nav-descriptions.ts exists at packages/ui/src/constants/nav-descriptions.ts with 56 route path keys
- CustomizePage.tsx exists at packages/ui/src/pages/CustomizePage.tsx with named export
- TypeScript typecheck passes with zero new errors
- CustomizePage uses usePinnedItems, useToast, NAV_DESCRIPTIONS, navGroups
- Pin limit of 15 enforced with warning toast
- Search filters by label client-side
- Pin counter shows "X / 15 slots used"
- data-testid attrs on search input, pin counter, and grid wrapper
</success_criteria>

<output>
After completion, create `/home/ayaz/ownpilot/.planning/phases/03-customize-page/03-01-SUMMARY.md`
following the template in @$HOME/.claude/get-shit-done/templates/summary.md
</output>
