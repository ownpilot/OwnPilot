---
phase: 03-customize-page
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - packages/ui/src/App.tsx
autonomous: false
requirements: [CZ-01, CZ-02, CZ-03, CZ-04, CZ-05]

must_haves:
  truths:
    - "Navigating to /customize in the browser renders the CustomizePage component inside the Layout"
    - "The Sidebar's always-visible Customize link navigates to /customize without a 404 or redirect"
    - "Pinning an item on /customize and navigating to any other page shows the pinned item in the sidebar"
  artifacts:
    - path: "packages/ui/src/App.tsx"
      provides: "Route registration: path 'customize' element CustomizePage"
      contains: "customize"
  key_links:
    - from: "packages/ui/src/App.tsx"
      to: "packages/ui/src/pages/CustomizePage.tsx"
      via: "lazy import + Route element"
      pattern: "CustomizePage"
---

<objective>
Register the /customize route in App.tsx and perform a human verification that the full Customize page flows work end-to-end.

Purpose: Without this route, the Sidebar's always-visible Customize link leads to a catch-all redirect back to /. This plan adds the lazy import and Route declaration, then verifies the whole pin/unpin UX works in the browser.

Output:
- packages/ui/src/App.tsx with CustomizePage lazy import and Route registered
- Human-verified: /customize loads, pin toggling works, sidebar reflects changes
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/ayaz/ownpilot/.planning/ROADMAP.md
@/home/ayaz/ownpilot/.planning/phases/03-customize-page/03-01-SUMMARY.md

<interfaces>
<!-- Key interfaces the executor needs. -->

From packages/ui/src/App.tsx (existing pattern — follow exactly):
```tsx
// 1. Lazy import at the top of the file (alongside other lazy imports):
const CustomizePage = lazy(() =>
  import('./pages/CustomizePage').then((m) => ({ default: m.CustomizePage }))
);

// 2. Route inside the AuthGuard > Layout route children (alongside other routes):
<Route path="customize" element={page(<CustomizePage />)} />
```

Placement guidance:
- Add the lazy import immediately after the ClawsPage import (last existing lazy import, line 180)
- Add the Route after the `<Route path="claws" ...>` line (line 257) and before the
  redirect block that starts with `<Route path="background-agents" ...>`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Register /customize route in App.tsx</name>
  <files>packages/ui/src/App.tsx</files>
  <read_first>
    - packages/ui/src/App.tsx (full file — understand current lazy imports and route list)
    - packages/ui/src/pages/CustomizePage.tsx (confirm named export is CustomizePage)
  </read_first>
  <action>
Add two edits to `packages/ui/src/App.tsx`:

**Edit 1 — Lazy import** (add after the ClawsPage import on line 180):
```tsx
const CustomizePage = lazy(() =>
  import('./pages/CustomizePage').then((m) => ({ default: m.CustomizePage }))
);
```

**Edit 2 — Route registration** (add after the claws route, before the background-agents redirect):
```tsx
<Route path="customize" element={page(<CustomizePage />)} />
```

Use the Edit tool for both changes. Do NOT rewrite the whole file.

Verify the named export in CustomizePage.tsx is exactly `CustomizePage` (not `default`).
If the export name differs, adjust the import destructuring accordingly.
  </action>
  <acceptance_criteria>
    - grep "CustomizePage" packages/ui/src/App.tsx: lazy import and Route both present
    - grep "path=\"customize\"" packages/ui/src/App.tsx: route with path "customize" exists
    - grep "import('./pages/CustomizePage')" packages/ui/src/App.tsx: lazy import present
  </acceptance_criteria>
  <verify>
    <automated>cd /home/ayaz/ownpilot && npx tsc --noEmit -p packages/ui/tsconfig.json 2>&1 | grep -E "App.tsx|CustomizePage|error TS" | head -10 || echo "typecheck clean"</automated>
  </verify>
  <done>App.tsx has CustomizePage lazy import and Route path="customize" registered. TypeScript compiles clean with zero new errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify — full Customize Page E2E flow</name>
  <files>none</files>
  <action>
Human verification gate — no code changes. Executor pauses here and presents the verification steps below to the user.
  </action>
  <verify>
    <automated>echo "Human checkpoint — verification performed by user"</automated>
  </verify>
  <done>User types "approved" confirming all 8 manual tests pass.</done>
  <what-built>
    Complete Phase 3 Customize Page:
    - nav-descriptions.ts: 56-item description map
    - CustomizePage.tsx: categorized card grid with search, pin toggle, pin counter, limit toast
    - App.tsx: /customize route registered with lazy import
  </what-built>
  <how-to-verify>
    Run the dev server first: `cd /home/ayaz/ownpilot && pnpm run dev`
    Then open http://localhost:5173 in a browser.

    **Test 1 — Route works (CZ-01)**
    - Click the "Customize" link in the sidebar (left sidebar, below pinned items)
    - Expected: /customize loads showing the page title "Customize Sidebar" and a grid of nav item cards

    **Test 2 — Grid is populated (CZ-01)**
    - Count the visible groups: Main, Personal Data, AI and Automation, Tools and Extensions, System, Experimental, Settings, Other
    - Expected: 8 groups visible, each with item cards

    **Test 3 — Pin toggle works (CZ-02, CZ-03)**
    - Click on any card that is NOT currently pinned (e.g., "Tasks")
    - Expected: card turns blue/primary color, shows filled pin icon
    - Navigate to / (home) and look at the sidebar
    - Expected: "Tasks" now appears in the pinned items section above the Customize link

    **Test 4 — Unpin works (CZ-02, CZ-03)**
    - Navigate back to /customize
    - Click the same "Tasks" card again
    - Expected: card returns to default gray, pin icon disappears
    - Navigate back to / — "Tasks" no longer appears in pinned items

    **Test 5 — Search works (CZ-04)**
    - Type "memo" in the search bar
    - Expected: grid filters to show only "Memories" card, all other groups/items hidden
    - Clear the search — all items return

    **Test 6 — Pin counter (CZ-05)**
    - Check the counter in the header: should show "X / 15 slots used"
    - Pin a few items — counter increments correctly

    **Test 7 — Pin limit toast (CZ-05)**
    - If you have fewer than 15 items pinned, pin items until you reach 15
    - Attempt to pin one more item
    - Expected: warning toast appears with text mentioning "max 15" or "pin limit"
    - The 16th item is NOT added to the pinned list

    **Test 8 — TypeScript check (final gate)**
    - Run in terminal: `cd /home/ayaz/ownpilot && pnpm run typecheck`
    - Expected: exits 0 with no errors (or only pre-existing errors unrelated to Phase 3 files)
  </how-to-verify>
  <resume-signal>
    Type "approved" if all 8 tests pass.
    Or describe any issues found (e.g., "Test 3 failed — clicking card does nothing").
  </resume-signal>
</task>

</tasks>

<verification>
Final checks after human approval:
1. Route registered: `grep "path=\"customize\"" /home/ayaz/ownpilot/packages/ui/src/App.tsx`
2. Lazy import present: `grep "CustomizePage" /home/ayaz/ownpilot/packages/ui/src/App.tsx`
3. TypeScript clean: `cd /home/ayaz/ownpilot && npx tsc --noEmit -p packages/ui/tsconfig.json 2>&1 | grep "error TS" | wc -l`
4. Phase 3 files committed: `git -C /home/ayaz/ownpilot log --oneline -5`
</verification>

<success_criteria>
- App.tsx registers Route path="customize" with lazy-loaded CustomizePage
- TypeScript typecheck passes with zero new errors
- Human-verified: /customize route renders the grid, pin/unpin works, sidebar reflects changes, search filters correctly, pin limit toast fires, pin counter is accurate
- Requirements CZ-01 through CZ-05 all verified by human tester
</success_criteria>

<output>
After completion, create `/home/ayaz/ownpilot/.planning/phases/03-customize-page/03-02-SUMMARY.md`
following the template in @$HOME/.claude/get-shit-done/templates/summary.md
</output>
